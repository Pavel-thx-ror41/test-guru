<nav class="navbar navbar-expand navbar-dark bg-dark">
  <div class="container">
    <%= link_to 'TestGuru', root_path, class: "navbar-brand" %>

    <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li class="nav-item"><%= link_to current_user.email, edit_user_registration_path, class: "nav-link" %></li>
        <li class="nav-item">
          <% if current_user&.admin? %><%= link_to 'Admin panel', admin_tests_path, class: "nav-link" %><% end %>
        </li>
        <li class="nav-item">
          <% if current_user&.admin? %><%= link_to 'Gists', admin_gists_path, class: "nav-link" %><% end %>
        </li>
        <li class="nav-item">
          <%= button_to t('devise.shared.links.log_out'), destroy_user_session_path, method: :delete, class: "nav-link btn btn-link" %>
        </li>
      <% else %>
        <li class="nav-item"><%= link_to t('devise.shared.links.log_in'), new_user_session_path, class: "nav-link" %></li>
        <li class="nav-item"><%= link_to t('devise.shared.links.sign_up'), new_user_registration_path, class: "nav-link" %></li>
      <% end %>
    </ul>
  </div>
</nav>
