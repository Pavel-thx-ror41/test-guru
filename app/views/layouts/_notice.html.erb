      <% if notice %>
      <p id="notice"><%= (notice.is_a?(Hash) && notice[:errors]) ?
                           to_ul(notice[:errors]) :
                           "<mark>#{notice}</mark>".html_safe %>
      </p>
      <% end %>
