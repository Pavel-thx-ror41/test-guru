<nav class='admin'>
  <%= link_to 'Тест-Гуру Admin Panel', admin_tests_path %>
  <div class="nav_user">
    <% if user_signed_in? %>
      Welcome, <b><%= current_user.email %></b> Guru
      <%= button_to 'Выйти', destroy_user_session_path, method: :delete, form_class: :logout_button_form, class: :button_as_href %>
    <% end %>
  </div>
</nav>
