ru:
    activerecord:
        errors:
            models:
                test:
                    attributes:
                        category:
                            required: "не может быть пустым"
